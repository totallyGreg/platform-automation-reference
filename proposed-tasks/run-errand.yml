---
platform: linux

inputs:
- name: config
- name: env

params:
  CONFIG_FILE: product.yml
  ERRAND_NAME:
  ENV_FILE: env.yml
  OPSMAN_SSH_PRIVATE_KEY:
  INSTANCE:

run:
  file: proposed-tasks/run-errand.yml
